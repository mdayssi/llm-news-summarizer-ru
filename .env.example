# Локальные пути/настройки среды
EXTERNAL_STORAGE_DIR=/path/to/external/storage  # локальная папка или смонтированный диск
DATA_DIR=./data                                 # рабочая папка данных внутри проекта
INDEX_DIR=./indexes                             # путь для индекса RAG
MODELS_DIR=./models                             # веса LoRA/DPO (внешнее хранилище)
